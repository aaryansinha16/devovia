[phases.setup]
aptPkgs = ["nodejs"]

[phases.install]
cmds = [
  # Use npm directly instead of pnpm
  "npm install && cd packages/database && npm run build && cd ../../apps/api && npm run build"
]

[start]
cmd = "node dist/index.js"
