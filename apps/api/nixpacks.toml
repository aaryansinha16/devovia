[phases.setup]
aptPkgs = ["nodejs", "git"]

[phases.install]
cmds = [
  "npm install --legacy-peer-deps"
]

[phases.build]
cmds = [
  "npm run prisma:deploy",
  "npm run build"
]

[start]
cmd = "node dist/index.js"
