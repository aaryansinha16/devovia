"use client";

/**
 * Cursor manager component - now deprecated as GlobalCursor handles all cursor behavior
 * Kept for backward compatibility but does nothing
 */
export default function CursorManager() {
  return null;
}
