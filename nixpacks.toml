[phases.setup]
aptPkgs = ["git"]
nixPkgs = ["pnpm", "nodejs_22"]

[phases.install]
cmds = [
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "cd apps/api && pnpm run build"
]

[start]
cmd = "cd apps/api && pnpm exec prisma migrate deploy && node dist/index.js"
